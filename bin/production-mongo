#!/bin/bash

DIRNAME=$(cd `dirname $0` && pwd)
USERNAME=$(<~/.meteor/quux.me.username)
PASSWORD=$(<~/.meteor/quux.me.password)
HOST="54.85.95.182"
PORT="27017"
DB="quux_me"
OUT_DIR="/tmp"

mongo -u $USERNAME -p $PASSWORD $HOST:$PORT/$DB
